# Episodes — GraphQL Queries

This folder contains GraphQL queries to fetch specific episodes from the Rick and Morty GraphQL API using the `episode(id: ID!)` field.

Each `episode-id-X.graphql` requests:
- id
- name
- air_date
- episode

Corresponding `episode-id-X-output.json` files should contain the API JSON response for each episode id.
